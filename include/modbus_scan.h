#pragma once
#include "config.h"

// Expose nothing more than the public API already in config.h.
// This header exists just to be explicit/clean; you can omit it
// if you include "config.h" everywhere.